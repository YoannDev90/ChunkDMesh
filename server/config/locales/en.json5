{
  en: {
    //api.py
    submit_tasks_received: "Received submission for batch {batch_id} with hashes: {chunk_hashes}",
    upload_chunks_received: "Received chunk data for batch {batch_id}, size: {chunk_size} bytes, SHA-256: {sha256_hash}",
    tile_uploaded: "Tile uploaded for batch {batch_id}, region: {region}, scale: {scale}",
    http_request: "{method} {path} -> {status} ({ms}ms)",
    tile_served: "Served tile r.{rx}.{rz} s{scale} from cache",
    tile_render_fallback: "Rendering tile r.{rx}.{rz} s{scale} from .mca (fallback)",
  },
}
